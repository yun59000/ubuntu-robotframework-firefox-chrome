image: docker:latest

services:
  - docker:dind

variables:
  DOCKER_DRIVER: overlay

build:
  stage: build
  script:
    - docker build -t test . 
    - uzWU4uKQfS2cKNpRsJEx
    - docker login -u yves -p uzWU4uKQfS2cKNpRsJEx $CI_REGISTRY
    - docker push test
