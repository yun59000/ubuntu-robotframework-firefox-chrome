image: docker:latest

services:
  - docker:dind

variables:
  DOCKER_DRIVER: overlay

build:
  stage: build
  script:
    - docker build -t test . 
    - docker login -u $CI_REGISTRY_USER -p $CI_REGISTRY_PASSWORD $CI_REGISTRY
    - docker push test
